export interface ServerConfig {
    vite: {
        port: number;
        hmr: {
            protocol: string;
            host: string;
            port: number;
            clientPort: number;
        };
        cors: boolean;
        headers: {
            "Access-Control-Allow-Origin": string;
        };
    };
    logging: {
        level: string;
        prefix: string;
    };
    client: {
        logging: {
            level: string;
            prefix: string;
            enableRemoteLogging: boolean;
        };
        viteServerUrl: string;
    };
    server: {
        editCooldownMs: number;
    };
}
export declare function defineConfig<T extends Partial<ServerConfig>>(config: T): T;
export declare const getConfigFromFile: () => Promise<any>;
export declare function getServerConfig(): Promise<ServerConfig>;
/**
 * @deprecated - Use getServerConfig instead
 */
export declare const serverConfig: ServerConfig;
